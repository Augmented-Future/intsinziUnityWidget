class Leaderboard {
  int rank;
  String avatar, name, school;
  double points;
  Leaderboard(
      {required this.rank,
      required this.avatar,
      required this.name,
      required this.school,
      required this.points});
}
